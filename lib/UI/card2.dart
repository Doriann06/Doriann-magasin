import 'package:flutter/material.dart';
 class Ecran2 extends StatelessWidget {
 @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.lightGreenAccent,
      child: const Center(child: Text('Ecran 3')),
    );
  }
}